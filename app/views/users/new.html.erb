<div class="container centrado">
	<h1>Crear usuario</h1>
	<div class="row ">
		<div class="centrado">
			<%= form_for @user do |f| %>
			<% if @user.errors.any? %>
				<% @user.errors.full_messages.each do |msg| %>
					<ul>
						<li><%= msg %></li>
					</ul>
				<% end %>
			<% end %>
			<div class="form-group">
				<%= f.label :Correo %>
				<%= f.text_field :email, class:"form-control", placeholder:"Ingrese su email" %>			
			</div>

			<div class="form-group">
				<%= f.label :Contraseña %>
				<%= f.password_field :password, class:"form-control", placeholder:"Ingrese password" %>			
			</div>

			<div class="form-group">
				<%= f.label :nombre %>
				<%= f.text_field :name, class:"form-control", placeholder:"Ingrese su name" %>			
			</div>

			<%= f.submit 'Crear usuario', class:"btn btn-primary"%>
			
		<% end %>
			
		</div>
		
	</div>
	<div class="row flecha" >
		<span><%= link_to 'Atras', root_path, class:"fas fa-arrow-circle-left" %></span>
	</div>
</div>